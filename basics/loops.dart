main() {
  /// for
  int count = 0;
  for (count; count < 10;) {
    if (count == 8) {
      break;
    }

    if (count == 5) {
      count = count + 1;

      continue;
    }

    print(count);
    // count++;
    count = count + 1;
  }

  print(" please run this");

  // print(count);

  /// forEach

  /// for in
}

// for , while, do while, break, continue
